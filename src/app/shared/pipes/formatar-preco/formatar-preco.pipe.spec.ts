import { FormatarPrecoPipe } from './formatar-preco.pipe';

describe('FormatarPrecoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarPrecoPipe();
    expect(pipe).toBeTruthy();
  });
});
